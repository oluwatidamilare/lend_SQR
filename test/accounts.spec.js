const assert = require('assert')

describe('accounts', () => {
	it('addAccount() ', () => {
		let result = 1 + 1
		assert.equal(result, 2, 'these numbers are equal')
	})
    it('getAllAccounts()', () => {
		let result = 1 + 1
		assert.equal(result, 2, 'these numbers are equal')
	})
    it('findAccount() ', () => {
		let result = 1 + 1
		assert.equal(result, 2, 'these numbers are equal')
	})

})
