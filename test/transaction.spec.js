const assert = require('assert')

describe('transaction', () => {
	it('createAccount() ', () => {
		let result = 1 + 1
		assert.equal(result, 2, 'these numbers are equal')
	})
    it('fundAccount()', () => {
		let result = 1 + 1
		assert.equal(result, 2, 'these numbers are equal')
	})
    it('withdrawFunds() ', () => {
		let result = 1 + 1
		assert.equal(result, 2, 'these numbers are equal')
	})
})
