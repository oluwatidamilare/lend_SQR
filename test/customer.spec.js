const assert = require('assert')

describe('customer', () => {
	it('getAccountNo() should return a 10-digit string', () => {
		let result = 1 + 1
		assert.equal(result, 2, 'these numbers are equal')
	})
    it('getAccountBalance() should not return a negative account balance', () => {
		let result = 1 + 1
		assert.equal(result, 2, 'these numbers are equal')
	})
    it('increaseBalance() should not decrease the balance', () => {
		let result = 1 + 1
		assert.equal(result, 2, 'these numbers are equal')
	})
    it('decreaseBalance() should not increase the balance', () => {
		let result = 1 + 1
		assert.equal(result, 2, 'these numbers are equal')
	})
})
